services:
  - type: worker
    name: studyroom-push-relay
    runtime: node
    rootDir: services/studyroom-push-relay
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FIREBASE_PROJECT_ID
        value: algoviz-plus
      - key: FIREBASE_SERVICE_ACCOUNT_JSON_BASE64
        sync: false
